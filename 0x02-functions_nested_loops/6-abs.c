#include "main.h"
/**
 * _abs -functions that compile abasolute vale of an integer
 *@r: the value
 * Description: The standard library that provides a similar functions
 *
 * Return: (0)
 *
 */
int _abs(int r)
{
	if (r >= 0)

		return (r);
	else
		return (r * -1);
}
